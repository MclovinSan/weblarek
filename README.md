# Проектная работа "Веб-ларек"

Стек: HTML, SCSS, TS, Vite

Структура проекта:
- src/ — исходные файлы проекта
- src/components/ — папка с JS компонентами
- src/components/base/ — папка с базовым кодом

Важные файлы:
- index.html — HTML-файл главной страницы
- src/types/index.ts — файл с типами
- src/main.ts — точка входа приложения
- src/scss/styles.scss — корневой файл стилей
- src/utils/constants.ts — файл с константами
- src/utils/utils.ts — файл с утилитами

## Установка и запуск
Для установки и запуска проекта необходимо выполнить команды

```
npm install
npm run dev
```

или

```
yarn
yarn dev
```
## Сборка

```
npm run build
```

или

```
yarn build
```
# Интернет-магазин «Web-Larёk»
«Web-Larёk» — это интернет-магазин с товарами для веб-разработчиков, где пользователи могут просматривать товары, добавлять их в корзину и оформлять заказы. Сайт предоставляет удобный интерфейс с модальными окнами для просмотра деталей товаров, управления корзиной и выбора способа оплаты, обеспечивая полный цикл покупки с отправкой заказов на сервер.

## Архитектура приложения

Код приложения разделен на слои согласно парадигме MVP (Model-View-Presenter), которая обеспечивает четкое разделение ответственности между классами слоев Model и View. Каждый слой несет свой смысл и ответственность:

Model - слой данных, отвечает за хранение и изменение данных.  
View - слой представления, отвечает за отображение данных на странице.  
Presenter - презентер содержит основную логику приложения и  отвечает за связь представления и данных.

Взаимодействие между классами обеспечивается использованием событийно-ориентированного подхода. Модели и Представления генерируют события при изменении данных или взаимодействии пользователя с приложением, а Презентер обрабатывает эти события используя методы как Моделей, так и Представлений.

### Базовый код

#### Класс Component
Является базовым классом для всех компонентов интерфейса.
Класс является дженериком и принимает в переменной `T` тип данных, которые могут быть переданы в метод `render` для отображения.

Конструктор:  
`constructor(container: HTMLElement)` - принимает ссылку на DOM элемент за отображение, которого он отвечает.

Поля класса:  
`container: HTMLElement` - поле для хранения корневого DOM элемента компонента.

Методы класса:  
`render(data?: Partial<T>): HTMLElement` - Главный метод класса. Он принимает данные, которые необходимо отобразить в интерфейсе, записывает эти данные в поля класса и возвращает ссылку на DOM-элемент. Предполагается, что в классах, которые будут наследоваться от `Component` будут реализованы сеттеры для полей с данными, которые будут вызываться в момент вызова `render` и записывать данные в необходимые DOM элементы.  
`setImage(element: HTMLImageElement, src: string, alt?: string): void` - утилитарный метод для модификации DOM-элементов `<img>`


#### Класс Api
Содержит в себе базовую логику отправки запросов.

Конструктор:  
`constructor(baseUrl: string, options: RequestInit = {})` - В конструктор передается базовый адрес сервера и опциональный объект с заголовками запросов.

Поля класса:  
`baseUrl: string` - базовый адрес сервера  
`options: RequestInit` - объект с заголовками, которые будут использованы для запросов.

Методы:  
`get(uri: string): Promise<object>` - выполняет GET запрос на переданный в параметрах ендпоинт и возвращает промис с объектом, которым ответил сервер  
`post(uri: string, data: object, method: ApiPostMethods = 'POST'): Promise<object>` - принимает объект с данными, которые будут переданы в JSON в теле запроса, и отправляет эти данные на ендпоинт переданный как параметр при вызове метода. По умолчанию выполняется `POST` запрос, но метод запроса может быть переопределен заданием третьего параметра при вызове.  
`handleResponse(response: Response): Promise<object>` - защищенный метод проверяющий ответ сервера на корректность и возвращающий объект с данными полученный от сервера или отклоненный промис, в случае некорректных данных.

#### Класс EventEmitter
Брокер событий реализует паттерн "Наблюдатель", позволяющий отправлять события и подписываться на события, происходящие в системе. Класс используется для связи слоя данных и представления.

Конструктор класса не принимает параметров.

Поля класса:  
`_events: Map<string | RegExp, Set<Function>>)` -  хранит коллекцию подписок на события. Ключи коллекции - названия событий или регулярное выражение, значения - коллекция функций обработчиков, которые будут вызваны при срабатывании события.

Методы класса:  
`on<T extends object>(event: EventName, callback: (data: T) => void): void` - подписка на событие, принимает название события и функцию обработчик.  
`emit<T extends object>(event: string, data?: T): void` - инициализация события. При вызове события в метод передается название события и объект с данными, который будет использован как аргумент для вызова обработчика.  
`trigger<T extends object>(event: string, context?: Partial<T>): (data: T) => void` - возвращает функцию, при вызове которой инициализируется требуемое в параметрах событие с передачей в него данных из второго параметра.

### Типы данных

#### Интерфейс IProduct
- Содержит типы данных товаров

Типы данных:
`id: string;`
`description: string;`
`image: string;`
`title: string;`
`category: string;`
`price: number | null;`


#### Интерфейс IBuyer
- Содержит типы для вводных данных пользователей

Типы данных:
`payment: TPayment;`
`email: string;`
`phone: string;`
`address: string;`

#### Интерфейс IProductsResponse
- Содержит типы данных для получения с сервера

типы данных:
`total: number;`
`items: IProduct[];`

#### Интерфейс IOrder
- Содержит типы данных пользователя для отправки на сервер

Включает в себя интерфейс IBuyer
Типы данных:
`payment: TPayment; `
`email: string;`
`phone: string;`
`address: string;`
`total: number;`
`items: string[];`

#### Интерфейс IOrderSuccesResponse
- Содержит типы данных ответа после отправки на сервер данных пользователя

Типы данных:
`id: string;`
`total: number;`

### Модели данных

#### Класс Catalog
хранение товаров, которые можно купить в приложении

Поля класса:
`protected _products: IProduct[] = []` - Хранит массив всех товаров
`private _preview: IProduct | null = null` - Хранит товар выбранный для подробного отображения

Конструктор класса не принимает параметров.

Методы класса:
`setItems(items: IProduct[]): void` - сохранение массива товаров полученного в параметрах метода. Принимает объект со всеми данными о продукте
`getItems(): IProduct[]` - получение массива товаров
`getItemById(id: string): IProduct | undefined` - получение одного товара по его id. Принимает id, возвращает все данные о продукте, либо undefined
`setPreview(product: IProduct): void` - сохранение товара для подробного отображения. Принимает объект со всеми данными о продукте
`getPreview(): IProduct | null`  - получение товара для подробного отображения


#### Класс корзина
хранение товаров, которые пользователь выбрал для покупки

Поля класса:
`protected _products: IProduct[] = []` - хранит массив товаров, выбранных покупателем для покупки.

Конструктор класса не принимает параметров.

Методы класса:
`getItems(): IProduct[]` - получение массива товаров, которые находятся в корзине
`addItem(product: IProduct): void` - добавление товара, который был получен в параметре, в массив корзины. Принимает все данные о товаре
`deleteItem(product: IProduct): void` - удаление товара, полученного в параметре из массива корзины. Принимает все данные о товаре
`clearItems(): void` - очистка корзины
`totalPrice(): number` - получение стоимости всех товаров в корзине
`countItems(): number` - получение количества товаров в корзине
`haveItem(id: string): boolean` - проверка наличия товара в корзине по его id, полученного в параметр метода

#### Класс покупатель
данные покупателя, которые тот должен указать при оформлении заказа

Конструктор класса не принимает параметров.

Поля класса:
`payment: IBuyer['payment'] = ''` - способ оплаты
`email: string = ''` - почта
`phone: string = ''` - телефон
`address: string = ''` - адресс

Методы класса:
`setBuyerData(data: Partial<IBuyer>): void` - сохранение данных в модели. Принимает на вход объект с полями интерфейса IBuyer 
`getBuyerData(): IBuyer` - получение всех данных покупателя
`clearBuyerData(): void` - очистка данных покупателя
`validateData(data: IBuyer)` - валидация данных. На вход принимает все данные пользователя

### Слой коммуникации

### Класс LarekApi
Этот класс будет использовать композицию, чтобы выполнить запрос на сервер с помощью метода get класса Api и будет получать с сервера объект с массивом товаров.

`constructor(baseApi: IApi)` - конструктор принимает экземпляр класса Api

методы: 
`getProduct(): Promise<IProduct[]>` - возвращает промис с массивом товаров
`postOrder(order: IPost): Promise<IOrderSuccesResponse>` - отправляет данные пользователя на сервер

### Слой view

#### Класс Header
Представляет шапку сайта

Наследуется от класса Component

в конструктор принимает блок header и ивент брокер

поля :
`basketButton: HTMLButtonElement` - кнопка открытия корзины
`counterElement: HTMLElement` - счетчик товаров в корзине

методы:

`set counter(value: number)` - изменяет число на счетчике

#### Класс Gallery
Представляет собой каталог

Наследуется от класса Component

в конструкторе принимает сам элемент каталога

поля:
`catalogElement: HTMLElement` - элемент каталога

методы:
`set catalog(items: HTMLElement[])` - отрисовка элементов

#### Класс Card
Абстрактный класс родитель для классов карточек

Наследуется от класса Component

В конструкторе принимает контейнер карточки

Поля класса:
`cardPriceElement: HTMLElement` - элемент отображения цены
`cardTitleElement: HTMLElement` - элемент отображения названия

Методы класса:
`set price(value: number | null)` - устанавливает стоимость товара
`set title(value: string)` - устанавливает название товара

#### Класс CardCatalog
Реализует отображение карточки в каталоге

Наследуется от класса Card

В конструкторе принимает контейнер и объект с обработчиком клика

Поля класса:
`imageEl: HTMLImageElement` - элемент изображения товара
`categoryEl: HTMLElement` - элемент категории товара

Методы класса:
`set image(value: string)` - устанавливает ссылку на изображение
`set category(value: string)` - устанавливает категорию и стиль
#### Класс CardPreview
Реализует отображение подробной информации о карточке в модальном окне

Наследуется от класса Card

В конструкторе принимает контейнер и объект с обработчиком клика

Поля класса:
`descriptionEl: HTMLElement` - элемент с описанием товара
`actionButton: HTMLButtonElement` - кнопка добавления или удаления
`imageEl: HTMLImageElement` - элемент изображения товара
`categoryEl: HTMLElement` - элемент категории товара

Методы класса:
`set image(value: string)` - устанавливает ссылку на изображение
`set category(value: string)` - устанавливает категорию и стиль
`set description(value: string)` - устанавливает текст описания
`set valid(isOrdered: boolean)` - меняет текст кнопки действия
`set price(value: number | null)` - проверяет цену на бесценность

#### Класс CardBasket
Реализует отображение карточки товара в корзине

Наследуется от класса Card

В конструкторе принимает контейнер и объект с обработчиком клика

Поля класса:
`deleteButtonElement: HTMLButtonElement` - кнопка удаления товара
`basketIndexElement: HTMLElement` - элемент порядкового номера

Методы класса:
`set index(value: number)` - устанавливает порядковый номер товара

#### Класс Basket
Реализует отображение корзины с выбранными товарами

Наследуется от класса Component

В конструкторе принимает контейнер и объект с обработчиком клика

Поля класса:
`basketListEl: HTMLElement` — элемент списка товаров
`totalPriceEl: HTMLElement` — элемент отображения общей стоимости
`orderButton: HTMLButtonElement` — кнопка перехода к оформлению

Методы класса:
`set totalPrice(value: number)` — устанавливает итоговую сумму заказа
`set basketList(items: HTMLElement[])` — отрисовывает список и управляет кнопкой

#### Класс Modal
Реализует работу модального окна

Наследуется от класса Component

В конструкторе принимает контейнер окна

Поля класса:
`contentElement: HTMLElement` - контейнер для содержимого
`closeButtonElement: HTMLButtonElement` - кнопка закрытия окна

Методы класса:
`set content(item: HTMLElement)` - заменяет содержимое окна
`open()` - отображает модальное окно
`close()` - скрывает модальное окно

#### Класс Success
Реализует отображение окна успешного оформления заказа

Наследуется от класса Component

В конструкторе принимает контейнер и брокер событий

Поля класса:
`orderDescriptionElement: HTMLElement` - элемент с описанием списанной суммы
`orderSuccessClose: HTMLButtonElement` - кнопка закрытия окна

Методы класса:
`set price(value: number)` - устанавливает текст с итоговой стоимостью

#### Класс Form
Абстрактный класс для работы с формами

Наследуется от класса Component

В конструкторе принимает контейнер формы и брокер событий

Поля класса:
`submitButton: HTMLButtonElement` — кнопка отправки формы
`errorsElement: HTMLElement` — элемент для вывода ошибок

Методы класса:
`set errors(value: string)` — устанавливает текст ошибки
`set valid(value: boolean)` — управляет доступностью кнопки отправки

#### Класс OrderForm
Реализует форму выбора способа оплаты и ввода адреса

Наследуется от класса Form

В конструкторе принимает контейнер формы и брокер событий

Поля класса:
`buttons: HTMLButtonElement[]` — массив кнопок выбора способа оплаты

Методы класса:
`set payment(value: string)` — переключает активный класс кнопок оплаты

#### Класс ContactsForm

Реализует форму ввода контактных данных пользователя

Наследуется от класса Form

В конструкторе принимает контейнер формы и брокер событий

Класс не имеет собственных полей и методов, используя базовую логику родительского класса Form для обработки ввода данных и валидации.

### События

#### События изменения данных

`items:changed `- срабатывает при обновлении списка товаров в модели. Инициирует перерисовку главной галереи.

`preview:changed` - срабатывает при выборе товара для предпросмотра. Открывает модальное окно с подробным описанием.

`basket:change` - срабатывает при изменении состава корзины. Обновляет счетчик товаров в шапке сайта.

`buyerData:change` - срабатывает при любом изменении данных покупателя. Запускает валидацию форм заказа и контактов.

#### События интерфейса
`card:select` - выбор карточки товара в каталоге. Передает данные карточки для установки в превью.

`actionCardButton:click` - нажатие кнопки добавления/удаления товара в окне предпросмотра.

`basket:open` - нажатие на иконку корзины. Открывает модальное окно корзины.

`order:open` - переход к оформлению заказа из корзины. Открывает первую форму

`payment:change` - выбор способа оплаты

`order.address:change` - ввод данных в поле адреса. Передает значение в модель покупателя.

`order:update` - сабмит первой формы заказа

`contacts.email:change` / `contacts.phone:change` - ввод данных в поля почты и телефона

`contacts:update` - сабмит формы контактов запускает процесс отправки заказа на сервер

`modal:close` - закрытие модального окна